development:
  sessions:
    default:
      database: series_jump_development
      hosts:
        - localhost:27017
      options:
  options:
development:
  sessions:
    default:
      database: series_jump_development
      hosts:
        - localhost:27017
      options:
  options:
test:
  sessions:
    default:
      database: series_jump_test
      hosts:
        - localhost:27017
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0
